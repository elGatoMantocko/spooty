<div class="list-group">
  {{#each rooms}}
    <a href="#" class="list-group-item list-group-item-action" data-context-uri="{{uri}}" data-start-time="{{start_time}}">
      {{name}} - {{owner_name}}
    </a>
  {{/each}}
</div>
