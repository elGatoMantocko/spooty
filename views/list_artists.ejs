<h2>Artists</h2>
<% for(var i = 0; i < artists.length; i++) { %>
  <% if(artists[i] != "") { %>
    <a href="#" onClick="load_album_list('<%= artists[i] %>')"><%= artists[i] %></a>
    <%= artists[i] %>
    <br>
  <% } %>
<% } %>
